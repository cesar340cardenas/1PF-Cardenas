import { CabeceraTablaDirective } from './cabecera-tabla.directive';

describe('CabeceraTablaDirective', () => {
  it('should create an instance', () => {
    const directive = new CabeceraTablaDirective();
    expect(directive).toBeTruthy();
  });
});
